import { Meta } from '@storybook/blocks';
import { DesignTokenDocBlock } from 'storybook-design-token';
import '../../theme/default.css';

<Meta title="Design Tokens/Farger" />

### Token for farger

<DesignTokenDocBlock
  categoryName="SemanticColors"
  maxHeight={10000}
  viewType="table"
  showValueColumn={false}
  presenters={{ Color: ColorPresenter }}
/>

export function ColorPresenter({ token }) {
  return (
    <div
      style={{
        background: token.value,
        borderRadius: 2,
        height: 32,
        width: '100%',
        border: `solid 2px var(--fhi-color-neutral-border-subtle)`,
      }}
    ></div>
  );
}
